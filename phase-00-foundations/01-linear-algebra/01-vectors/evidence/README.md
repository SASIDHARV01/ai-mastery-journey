# Vectors evidence

`example-output.txt` contains actual output captured when the mentor ran `../example.py`. Arithmetic, the calculated unit-vector length, and the zero-vector branch were checked.

This is a mentor-provided teaching example. It is not a learner-built project, and it does not establish the learner's understanding or confirm that they ran the code.

After the learner builds the separately assigned project, record its run command, actual output, worked example, and limitations here. Learner project completion and publication remain pending.
